apiVersion: v1
kind: Secret
metadata:
  name: devsecops-app-secret
  namespace: dev
type: Opaque
# Values are base64 encoded — replace with real secrets
# echo -n 'your-secret' | base64
data:
  app-secret: Y2hhbmdlbWU=
